# Copy this to .env.local for local development
# Do NOT commit real keys into source control
GEMINI_API_URL=
GEMINI_API_KEY=
# Optional: service account JSON as a single-line escaped JSON string
# GEMINI_SA_KEY_JSON=
# Or point to a local file path (not checked into git)
# GEMINI_SA_KEY_PATH=
